using UnityEngine;

// 100% placeholder, I have no idea how modules are going to work code-wise, but I know they'll need to exist as items
[CreateAssetMenu(fileName = "ModuleItem", menuName = "ScriptableObjects/ModuleItem", order = 1)]
public class ModuleItem : EquippableItem
{
}
